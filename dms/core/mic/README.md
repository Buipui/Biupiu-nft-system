# Main Intelligence Control

Central policy decision point for identity, scope, product capability and entitlement evaluation. Deny by default; every protected decision emits an audit correlation ID.
