# Search Protocol Audit README — AI-81

This README is the canonical operating entry point for distinguishing search protocols by meaning rather than shared wording.

## Start here

1. Read `BIUPIU-AI81-HISTORICAL-SEARCH-PROTOCOL-AUDIT-v1.0.md`.
2. Load `BIUPIU-SEARCH-PROTOCOL-REGISTRY-AI81-v1.json`.
3. Read `../intelligence/BIUPIU-AI81-LEARNING-LOG.md`.
4. Read `BIUPIU-VERSION-CHANGELOG-AI81-2026-09-21.md`.
5. Use `BIUPIU-BLOCKCHAIN-ANCHOR-AI81.json` only as a prepared anchor manifest, never as proof of an on-chain transaction.

## Non-equivalence rule

The terms **search**, **harvest**, **federation**, **research**, and **audit** are not interchangeable. Classify each instruction by semantic intent, source lane, retrieval unit, language strategy, evidence model, promotion gate, and downstream consumer.

## Mandatory source record

Every extracted instruction must preserve its file path, heading, commit SHA, date context, original wording reference, normalized semantic fingerprint, supersession links, conflicts, implementation state, and verification state.

## Hard safety rules

- Repository-first; external discovery follows provenance checks.
- Search results never execute code or become evidence automatically.
- No credentials, secrets, private keys, confidential data, or unpublished IP.
- No authentication, paywall, licence, robots, or access-control bypass.
- No automatic promotion from `DISCOVERED` to `IMPLEMENTED` or `VERIFIED`.
- A blockchain manifest is not a transaction.

## Coverage statement

An audit must state whether it inspected all refs and reachable history. Default-branch content alone cannot establish complete historical coverage.
