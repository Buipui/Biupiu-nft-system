# OS-G07 Storage / Filesystem Contract v1.0
Status: CONTRACT IMPLEMENTED; runtime OPEN

Define block-device abstraction, filesystem mount/unmount, file handles, permissions, durability and recovery semantics. Filesystem implementation remains replaceable; compatibility adapters may expose POSIX-style paths.