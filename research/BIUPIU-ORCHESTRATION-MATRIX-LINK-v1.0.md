# BIUPIU ORCHESTRATION <-> CODING MATRIX LINK v1.0

Status: IMPLEMENTED

Digital Orchestra is separate; the Native Coding Philosophy & Engineering Matrix remains the governing engineering standard.

## Enforced/recorded
requirement/provenance; evidence state; language/runtime assignment; dependency/licence; schema/contract validation; static/type/test gates; reproducible build; runtime verification; regression; promotion authority.

## Route
Research -> Intelligence -> Orchestra -> OS/DMS validation -> domain execution -> verification -> digest/filing -> learning -> release.

## Language routing
C = ABI/HAL
Rust = safety/concurrency/security-sensitive native services
C++ = simulation/geometry/graphics/scientific subsystems
Python = deterministic orchestration/analysis/ML where appropriate
TypeScript = typed web/integration
Kotlin = Android
Solidity = minimal provenance/token boundary

## Identifiers
ORCH-<workflow>; CAP-<domain>-<name>; ART-<hash>; EVT-<uuid>; GATE-L<n>.

## Promotion
DISCOVERED -> CLASSIFIED -> PLANNED -> ROUTED -> EXECUTING -> OBSERVED -> VERIFIED -> DIGESTED -> FILED -> LEARNED -> PROMOTED

Failed security, licence, provenance or validation gates route to QUARANTINED.

## 2026-09-24 — Family Resource Split Contract

The Coding Matrix now consumes the Orchestra family envelope:
`family_id -> software_tree -> language/runtime -> ABI -> dependencies/licence -> OEM/provider -> hardware -> optimisation -> learning_graph -> build/test -> provenance -> authority -> evidence -> promotion`.

The split is by complete software-family stack. Shared services are referenced through contracts and are not duplicated. The eight specialist resource teams remain allocation groups; they do not replace the canonical four Federation AI identities.
