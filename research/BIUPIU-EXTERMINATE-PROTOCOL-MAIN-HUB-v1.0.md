# Biupiu Exterminate Protocol — Main Hub Gate v1.0

## Purpose
Non-destructive repository hygiene and conflict prevention before advancing the Main Hub runtime.

## Actions executed
- Searched existing Main Hub, environment, navigation and resource-index records.
- Checked for duplicate/overlapping Main Hub implementations before adding a new client layer.
- Confirmed the repository already contains a separate `packages/biupiu-main-hub/` shell and the newer executable `world/client/` prototype.
- Refreshed Babylon.js runtime package references to 9.27.1.
- Added a runtime resource manifest.
- Added a fail-closed destination route boundary.
- Updated the research index.

## Safety rules
- No repository files were destructively deleted by this protocol.
- Third-party resources remain references/dependencies until licence, security and compatibility checks pass.
- Client routing cannot grant protected access.
- Confidential/private material must not be inferred as protected merely from directory names.

## Next gate
Connect the existing Main Hub scene to route intents, then load environment manifests through a controlled adapter. Local install/build/browser execution must be verified separately before claiming runtime PASS.
