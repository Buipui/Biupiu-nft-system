type buipui_core, domain;
type buipui_core_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(buipui_core)

# Deliberately minimal. Add permissions only from audited AVC evidence.
# Never assign coredomain or broad filesystem/device access.
