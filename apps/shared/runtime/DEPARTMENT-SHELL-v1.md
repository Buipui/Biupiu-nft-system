# Biupiu Department Application Shell v1

Gate 18 provides a common application shell for department modules.

Every native department screen receives four navigation areas: Overview, Tools, Research, and Settings.

The shell is a UI/runtime contract only. It does not grant entitlements and does not replace the server-side authorization boundary.

Department-specific functionality can be mounted into the Tools and Research areas in later gates without changing the launch contract.
