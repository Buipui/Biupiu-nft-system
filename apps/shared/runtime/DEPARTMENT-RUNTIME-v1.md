# Gate 13 — Department Runtime Adapters

The common lifecycle is: resolve package → check entitlement → create platform adapter → start department when authorized.

Android and Windows expose equivalent runtime states and package identities. The adapter does not replace server-side authorization.
