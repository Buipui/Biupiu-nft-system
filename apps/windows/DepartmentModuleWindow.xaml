<Window x:Class="Biupiu.Desktop.DepartmentModuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Biupiu Department Module" Height="680" Width="1100">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" FontSize="30" HorizontalAlignment="Center"/>
        <TextBlock x:Name="IdentityText" Grid.Row="1" Margin="0,10,0,12" HorizontalAlignment="Center"/>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <UniformGrid x:Name="CapabilityPanel" Columns="2" Margin="0,8"/>
        </ScrollViewer>
        <TextBlock x:Name="StatusText" Grid.Row="3" Margin="8,16,8,0"
                   Text="READY" TextWrapping="Wrap" HorizontalAlignment="Center"/>
    </Grid>
</Window>
