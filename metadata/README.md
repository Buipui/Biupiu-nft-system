# NFT Metadata

Store finalized NFT metadata records here using the master template.

Recommended naming convention:

`BPU-NFT-YYYY-###.json`

Keep metadata versioned and preserve the exact release record used at minting.