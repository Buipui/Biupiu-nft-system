[package]
name = "biupiu-bevy-adapter"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Biupiu's original integration boundary for the Bevy Rust engine."

[dependencies]
bevy = { version = "0.19", default-features = false, features = ["bevy_asset", "bevy_scene"] }
