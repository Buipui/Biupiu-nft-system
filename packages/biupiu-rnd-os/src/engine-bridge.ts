export type EngineId = "UNREAL_ENGINE_5"|"UNITY"|"LUMION"|"TWINMOTION"|"BLENDER"|"GODOT"|"O3DE"|"WEB_XR"|"OTHER";
export type EngineHealth = "UNKNOWN"|"READY"|"DEGRADED"|"BLOCKED";
export interface EngineCapabilities { importFormats:string[]; exportFormats:string[]; supportsOpenXR:boolean; supportsOpenUSD:boolean; supportsGLTF:boolean; supportsDigitalTwinState:boolean; supportsPhysics:boolean; }
export interface EngineRegistration { engineId:EngineId; engineVersion:string; role:string; capabilities:EngineCapabilities; externalLicenseState:"PROPRIETARY_EXTERNAL"|"OPEN_SOURCE"|"INTERNAL"; }
export interface EngineHealthRecord { engineId:EngineId; checkedAt:string; state:EngineHealth; checks:string[]; failures:string[]; warnings:string[]; }
export const CANONICAL_CROSS_ENGINE_RULES={mainOsOwnsAuthoritativeState:true,digitalTwinOwnsVersionedSimulationMirror:true,providerStateCannotOverwriteSource:true,openStandardsPreferred:true,provenanceRequired:true,physicalStateSeparatedFromSimulatedState:true,directEngineToEngineMutation:false} as const;
export class EngineBridgeRegistry { private engines=new Map<EngineId,EngineRegistration>(); private health=new Map<EngineId,EngineHealthRecord>(); register(e:EngineRegistration){if(this.engines.has(e.engineId))throw new Error("duplicate engine registration: "+e.engineId);this.engines.set(e.engineId,e);} setHealth(h:EngineHealthRecord){if(!this.engines.has(h.engineId))throw new Error("health update for unregistered engine");this.health.set(h.engineId,h);} get(id:EngineId){return this.engines.get(id);} healthOf(id:EngineId){return this.health.get(id)??{engineId:id,checkedAt:new Date(0).toISOString(),state:"UNKNOWN",checks:[],failures:["NO_HEALTH_RECORD"],warnings:[]};} readiness(id:EngineId){return this.healthOf(id).state==="READY"?"READY":"NOT_READY";}}
