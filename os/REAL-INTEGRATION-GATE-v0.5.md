# Biupiu OS Real Integration Gate v0.5

The OS now has an adapter for an existing repository propulsion simulator. The original model remains in its existing location and is loaded at runtime rather than copied.

Verification target:
1. discover adapter;
2. execute existing model through kernel;
3. preserve model outputs;
4. apply evidence governance;
5. audit the execution.

This proves integration/orchestration, not physical correctness of the propulsion model.
