# Biupiu OS Cross-Domain Contract Gate v0.7

Adds a cross-domain validation boundary for values exchanged between existing simulator domains.

Current checks include completion state and basic physical-sign sanity checks. Unit primitives and conversion helpers are introduced for future dimensional validation.

This gate does not claim scientific correctness. It establishes the mechanism by which domain contradictions and malformed exchanges can be detected before downstream execution.
