# Biupiu OS Physics Guard Gate v0.8

Introduces dimensional quantities, unit conversion, dimensional mismatch detection, configurable parameter bounds, and cross-domain quantity validation.

These are guardrails around models, not proof that model equations are correct.

The next gate should apply the guards to an existing real simulator with explicit engineering bounds and conservation checks, then run the complete regression suite.
