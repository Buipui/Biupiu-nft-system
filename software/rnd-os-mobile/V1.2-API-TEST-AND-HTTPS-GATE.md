# Biupiu R&D OS Mobile — v1.2 API/Test/HTTPS Gate

## Executed

- Restored the R&D API adapter so dashboard requests delegate to the authenticated HTTP transport.
- Preserved bearer-token injection at the transport boundary.
- Kept the emulator HTTP endpoint explicitly development-only.
- Kept production HTTPS as a separate configuration boundary.
- Added/retained observable dashboard loading, error, empty, retry and refresh states.

## Verification boundary

GitHub read/write operations were used to update the mobile source. A local Android Gradle build, emulator run, or device test was not available in this execution, so compilation/runtime success is not asserted.

The repository currently does not expose a verified Android Gradle wrapper/build run through the available GitHub execution path. The next implementation gate should add CI-capable Android build infrastructure before treating the mobile client as build-verified.

## Next gate

Add deterministic transport/session tests, Android CI with Gradle wrapper, production HTTPS configuration, and reconnect dashboard navigation to Research and Digital Laboratory modules.
