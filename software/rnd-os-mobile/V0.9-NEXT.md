# Biupiu R&D OS Mobile — v0.9 Next Gate

The next mobile gate adds authenticated session UI, secure credential storage, research-object creation, experiment creation, evidence capture and offline synchronization.

The v0.8 transport remains development-only. Production builds must use HTTPS and a reviewed Android credential-storage strategy.
